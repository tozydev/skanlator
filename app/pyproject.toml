[project]
name = "app"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "core",
    "pyside6>=6.11.1",
]

[project.scripts]
app = "app:main"

[build-system]
requires = ["uv_build>=0.11.25,<0.12.0"]
build-backend = "uv_build"

[tool.uv.sources]
core = { workspace = true }
